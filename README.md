# 黎明时分：星存者 Web

React + Vite 前端架构，保留原有启动页视觉方向，后续可继续接入游戏场景、存档、设置、资源加载和路由。

## 运行

```bash
npm install
npm run dev
```

## 目录

```text
src/
  app/          应用入口、全局 Provider
  pages/        页面级组件
  features/     业务功能模块
  shared/       跨功能复用 UI、工具、类型
  styles/       全局样式与主题变量
```
